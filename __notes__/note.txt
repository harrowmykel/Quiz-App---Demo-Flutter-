Flutter{
    --> everything is a widget
}

App/Page Setup{
    --> Material ==> Android
    --> Cupertino ==> iOS
    {
        MaterialApp/CupertinoApp,
        Scaffold/CupertinoPageScaffold
    }
}

Map/Objects{
    --> collection of key:value pairs
    --> created with 
    `
        {
            a : value
        }
            OR

        Map()
    `
}

final/const{
    --> final for class properties
    --> const for method properties
}

Widget{
    --> created by flutter using the build(context) method in StatelessWidget
            or StatefulWidget
    --> displayed on screen
    --> Search flutter widget catalog for all widgets

    --> Types {
        --> visible Widgets (input, output content)
            --> buttons, text

        --> invisible Widgets
            --> columns, rows
    }
}

ForEach.Widget{
    -> use (List.map(item => Widget().toList())) tom implement a widget  creation loop
    --> Concatenate 2 lists
        `children: [
        Question(
            question: questions[_questionIndex],
        ),
        ...(questions[_questionIndex] as List<String>).map((answer) {
            return Answer(
            question: questions[_questionIndex],
            position: 0,
            answerFunction: _answerQuestion,
            );
        }).toList(),
        ], `
}
type casing{
    --> use (0.0 as int) to get same effect as 
    (int) 0.0
}
string interpolation{
    -->Text('${groupedTx['day']} : ${groupedTx['amount']}');
}

Abstraction/Access Modifier{
    --> when a class/Property starts with underscore, it can only be used in the 
    file it is defined in.
    {
        MyAppState, questionIndex => public
        _MyAppState, _questionIndex => private
    }

}

State{
    --> Data/info used by app
        --> e.g Variable etc
    --> widgets that just output data are usually stateless
     --> e.g Text Widget
    {
        --> App Wide State
        --> Widget State
    }
    --> force page reload by fixing it in setState (anonymous) function
        setState(() {
        questionIndex++;
        });
}

Statefulwidget{
    --> acces widget properties/fn inside state class with "widget."
    ` class MyApp extends StatefulWidget {
        @override
        State<StatefulWidget> createState() {
        return MyAppState();
        }
        void addTime(){}
      }
      class MyAppState extends State<MyApp>{
          widget.addTime();
      }
    `
}

Scaffold{
    ==> base page design
}

Column{
    Column(children: <Widget>[])
}

Container{
    --> HTML/CSS box model,
    --> container for widgets
}
Padding/Margin{
    -->use EdgeInsets.x for spacing
}
declarator{
    --> @override
}

pubspec.yaml{
    --> dependencies saving
}

main.dart{
    --> entry point for app
    -> import 'package:PACKAGE_NAME';
    -> import './question.dart';
}

functions{
    `void main(){
        runApp(MyApp());
    }
        OR
    //if only one expression, auto returned
    void main() => runApp(MyApp());
    `
}

getters and setter{
    --> getter function
        `List<Transaction> get _recentTransactions{
            return [];
        }`
    --> getter use case;
        `var x = _recentTransactions;` 

}

class{
    --> constructor parameters can be passed via variable name
    `MyApp(name: 'west', age: 21)`
    -> constructor can be executed like a function or with keyword 'new'
         e.g MyApp(); or new MyApp();
    `class ClassName{
        String name;
        int age;

        //constructors

        ClassName(int a, int b){
            //ClassName(23, 40);
        }
                OR
        ClassName({int a = 30, @required int b}){
            //ClassName(a: 23, b: 40);
            // @required is only available in Flutter not in dart
        }
                OR 
        ClassName({this.name, this.age});
                OR 
        ClassName.subConstructor(this.name){
            //set defaults
            age = 60;
        }
    }`
}

Scrolling{
    --> implement scrolling with 
    --> SingleChildScrollView
        --> requires a fixed parent height
        -> used directly in body of materialApp
    --> ListView in a Container with defined height
    --> ListView.builder() ==> RecyclerView()
}

Replacements for Deprecated/New Buttons{
    --> to change the material buttons colors use
    ElevatedButton{
        style: ButtonStyle{
            backgroundColor: MaterialStateProperty.all(Colors.blue)
        }
                    OR 
        style: ElevatedButton.styleFrom(
            primary: Colors.blue, //background
            onPrimary: Colors.red //foreground
        )
    }
}

Libraries{
    {
        intl -> handles date formatting
    }
}

X.of(context){
    --> use X.of(context) to get context properties
        --> Theme.of(context)
        --> 
}
Images{
    -->wrap images in container to fit block
}

SizedBox{
    --> use sizedbox to create margin between widgets
}

 Flexible{
    --> use Flexible to fit content around screen like 
    html css flex e.g
      `Flexible(
            //take all free space
            fit: FlexFit.tight,
                OR
            //take enough free space
            fit: FlexFit.loose,


            //set proportional width of unit chartbar
            flex: 1,
      )`
    --> Expanded ==> Flexible with fit: FlexFit.tight,
 }